---
# Named rules replace the deprecated numeric IDs (306/305/106).
skip_list:
  # The role is published as the namespaced "jtprogru.profile"; the CI checkout
  # path contains a dot, which the role-name rule would otherwise flag.
  - role-name

exclude_paths:
  - .github/
  - .venv/
